# ZULONG cross-platform audio/TTS dependencies.
# PyAudio depends on PortAudio system packages:
# - Windows: prefer a matching wheel if pip build fails.
# - Linux: install portaudio19-dev/python3-dev first.
# - macOS: install portaudio with Homebrew first.

pyaudio>=0.2.11
soundfile>=0.12.0
imageio-ffmpeg>=0.4.9

# TTS
kokoro>=0.9.0
edge-tts>=6.1.0

# SenseVoice ONNX runtime wrapper. Provider support is platform-specific.
sherpa-onnx>=1.10.0
funasr>=1.0.0
